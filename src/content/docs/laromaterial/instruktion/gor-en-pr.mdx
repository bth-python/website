---
title: "Gör en PR" 
description: Instruktion om hur du gör en pull request (PR) för att lämna in.
sidebar:
    order: 030
    # hidden: true
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';


Följ denna instruktionen för att göra en pull request (PR) för att lämna in uppgiften i kmom03, kmom06 och kmom10.

:::note
Bilderna visar inlämning för kmom03. Flödet är likadant för kmom06 och kmom10. I instruktionerna när det står `<X>`, ersätt det med `3` om du lämnar in kmom03 och `6` om du lämnar in kmom06.
:::

## Förutsättning

Du har uppfyllt kraven i uppgiften och har gjort en vanlig inlämning enligt [Resultat och redovisning](/laromaterial/instruktion/resultat-och-redovisning).



## Ordlista

- `PR`: Pull request
- `Tests passed(TP)`: Får din PR när de automatiska testerna gått igenom. Nu ska du bara vänta på att en lärare kollar på din inlämning.
- `Tests failed`: Får din PR när något gått fel i de automatiska testerna. Ladda ner testerna igen, fixa felet lokalt och pusha till GitHub.
- `Grade: G`: Helt klar med en uppgift.
- `Grade: Ux`: En lärare har bedömt din inlämning men tycker att något behöver förbättras i din inlämning. Följ instruktionerna, fixa problemet lokalt och pusha din lösning till GitHub. När du taggar så körs testerna på GitHub.
- `Grade: U`: En lärare har bedömt din inlämning men tycker inte att det är tillräckligt bra. Du behöver göra en ny inlämning till nästa rättningstillfälle.
- `Run tests`: Du ska sätta labeln "Run tests"  för att köra igång flödet som kör testerna och sätter `Tests Passed (TP)` eller `Tests failed`.
- `Integration failed`: Något i koden som kör de automatiserade testerna gick fel. Dubbelkolla att du är inloggad med SSO och har auktoriserat din SSH-nyckel. Kontakta en lärare.


## Mitt eget arbete

Det du lämnar in är ditt eget arbete. Om du har tagit hjälp av extern kod, eller om du har kopierat någon mindre del av koden, så är det tydligt angivet i kommentarerna i koden eller i din redovisningstext.

I samband med att du gör din PR så intygar du att arbetet är ditt eget och uppfyller de regler som finns för "[Fusk och medveten vilseledning](/studieguide/fusk/)".



## Pull request (PR)

Du skall nu göra din inlämning som en PR mot följande branch. En lärare rättar din PR på dessa inlämningsbrancher, exempelvis rättas kmom03 i inlämningsbranchen `bth/submit/kmom03`.

- **PR kmom03** mot `bth/submit/kmom03`
- **PR kmom06** mot `bth/submit/kmom06`
- **PR kmom10** mot `bth/submit/kmom10`

**Följ stegen nedan för att skapa en PR.**

### Gå till din kod på GitHub

Du gör detta med hjälp av GitHub. Se till att du har pushat ditt lokala kursrepo till GitHub och öppna ditt kursrepo på GitHub i webbläsaren.  
Klicka på "Pull requests" i navbaren.

import pr1ny from '@assets/pr/pr1ny.png';

<Figure 
    src={pr1ny}
    caption="Ditt kursrepo på GitHub."
/>



### Gå till Pull requests

När du har klickat på "Pull requests" i navbaren, så ser det ut så här.

import pr2ny from '@assets/pr/pr2ny.png';

<Figure 
    src={pr2ny}
    caption="Pull requests."
/>

Klicka på "New Pull Request".


### Förbered att skapa en ny PR

När du har klickat på "New Pull Request", så ser det ut så här.

import pr3 from '@assets/pr/pr3.png';

<Figure 
    src={pr3}
    caption="New pull requests innan du fyllt i dina val."
/>



### Välj brancher att jämföra

Nu skall du välja vilken branch du vill skapa en PR mot.

Din "base" skall vara `bth/submit/kmom0<X>` (3 för kmom03 och 6 för kmom06) (**1** i bilden).

Din "compare" skall vara `main` (**2** i bilden).

Det betyder att du vill merga `main` in till `bth/submit/kmom0X`.

Nu är du redo att skapa en PR, klicka på "Create pull request" (**3** i bilden).

import pr4ny from '@assets/pr/pr4ny.png';

<Figure 
    src={pr4ny}
    caption="Gör en PR för att merga main in till bth/submit/kmom03."
/>



### Öppna en PR mot en branch

När du har klickat på "Create pull request" så skall du fylla i detaljer om din PR:

Som **titel** skriver du "Inlämning av kmom0\<X\>"  (**1** i bilden).

Som **assignees** lägger du in dig själv. (**2** i bilden).

Nu kan du lägga till din PR genom att klicka på knappen "Create pull request" (**3** i bilden).

import pr5ny from '@assets/pr/pr5ny.png';

<Figure 
    src={pr5ny}
    caption="Gör en PR för att merga main in till bth/submit/kmom03."
/>


### En ny PR är öppnad

När din PR är öppnad kan det se ut så här:

import pr6ny from '@assets/pr/pr6ny.png';

<Figure 
    src={pr6ny}
    caption="En PR för inlämning av kmom03 har skapats."
/>


### Tester

Vänta ett par minuter, skrolla sen ner för att se att testerna har godkänts. Titta efter labeln Test passed (TP).

import pr7ny from '@assets/pr/pr7ny.png';

<Figure 
    src={pr7ny}
    caption="Lyckat resultat från testerna resulterar i Test passed (TP)."
/>

Du får även en sammanfattning på valideringen av taggarna och resultatet av testerna:

import pr8ny from '@assets/pr/pr8ny.png';

<Figure 
    src={pr8ny}
    caption="Lyckat resultat från testerna resulterar i denna sammanfattning."
/>


På bilden ovanför kan du se hur det ser ut när allt är OK. Testerna har lyckats och din inlämning är registrerad i Canvas.  
Om något gick fel, kanske fick du `Test failed` eller att taggarna inte var ok, se [Något fick fel](#något-gick-fel).

### Sista stegen (PR)

:::note
Nu har du två punkter kvar:

1. Kolla att du har fått `TP` i Canvas på kmom0\<X\> inlämningen (kan ta några minuter)
1. Skapa en branch för nästa inlämning, exempelvis har du precis lämnat in kmom03, skapar du en branch för kmom06.

```bash title="Skapa en ny branch för nästa inlämning"
git checkout -b bth/submit/kmom06 && git push origin bth/submit/kmom06
git checkout main
git branch # ok om stjärnan är framför main
````

Om du precis lämnat in kmom06, kör du ovanstående kommandon med **kmom10** istället för kmom06.
:::

#### Kontrollera nya branchen

Dubbelkolla att den nyss skapade branchen finns med:  

- i ditt lokala kursrepo; med  `git branch` då ska branchen synas, exempelvis om du precis skapade `bth/submit/kmom06` så syns den och `main` (main har en stjärna framför vilket visar att du är på main).  
När du har klickat så ser det ut så här:

import local_branches from '@assets/pr/local_branches.png';

<Figure 
    src={local_branches}
    caption="Dina brancher i ditt lokala git repo."
/>

- i ditt kursrepo på GitHub; klicka på "Branches" för att se att dina brancher.

import new_branch from '@assets/pr/new_branch.png';

<Figure 
    src={new_branch}
    caption="Klicka på Branches för att se dina brancher."
/>

När du har klickat så ser det ut så här:

import view_branches from '@assets/pr/view_branches.png';

<Figure 
    src={view_branches}
    caption="Dina brancher på GitHub."
/>


### Något gick fel

Om något gick fel med tester eller validering får du betyg `Test failed` på Canvas.

Om något gick fel med testerna får du en kommentar om vad som gick fel samt din PR får labeln `Tests failed` och PR:n stängs. Eftersom du kör testerna när du taggar så bör detta inte inträffa.

Vanliga fel:

- du har inte de nyaste filerna eller testerna
- du inte inloggad med SSO (logga ut och sen in igen och klicka på SSO-knappen på rätt organisation)

Nu behöver du kolla vad som gick fel, fixa felet och ladda upp koden igen.

#### Fixa dina fel

1. Ladda ner testerna på nytt och kör dem lokalt (kör `uv run task download-all`)
1. Rätta felet och testkör lokalt.
1. Pusha din rättning till GitHub och tagga den
1. Skapa en ny PR för att köra igång flödet som kör testerna och sätter `Tests Passed (TP)` eller `Tests failed`.


:::tip
Vi rättar endast inlämningar (PRs) med labeln `Tests Passed (TP)`.
:::


Om du får något annat konstigt fel du inte förstår, kolla om felet finns med nedanför i [Vanliga fel](#vanliga-fel) annars kontakta en lärare och be om hjälp.


## Feedback och bedömning

När läraren rättar din PR så får du återkoppling i kommentarer till din PR.

### Approved / G

Om läraren tycker att allt ser bra ut får du en kommentar, label `Approved` och `G` i Canvas. Lärare mergar och stänger då din PR. Du kan jobba vidare med nästa kmom.

### Needs improvement / Ux

Om läraren tycker att något är fel som du behöver fixa får du label `Needs improvement` och `Ux` på Canvas. Din PR är forsatt öppen.  
Det kommer finnas kommentarer på specifika kodrader. Om kommentaren innehåller en `Warning` som på bilden nedanför, då måste du åtgärda det i din kod. Om kommentaren inte innehåller `Warning` behöver du inte åtgärda det som står där.

**OBS** Låt kommentaren vara kvar! Om du vill svara på kommentaren, så går det bra.

import pr_warning from '@assets/pr/pr_comment_warning.png';

<Figure 
    src={pr_warning}
    caption="Kommentar som ska åtgärdas."
/>

När du har åtgärdat felen gör följande:
 
1. Rätta felet och testkör lokalt.
1. Pusha din rättning till GitHub och tagga den. Om din fix funkade ska du nu få labeln `Tests passed(TP)`.
1. Lägg till labeln `Run tests` på din öppna PR för att köra igång flödet som kör testerna och sätter `Tests Passed (TP)` eller `Tests failed`. Låt labeln `Grade: Ux` vara kvar.

:::note
Glöm inte att sätta labeln `Run tests` på din öppna PR!

Din komplettering ska lämnas in inom 5 arbetsdagar!
:::


### Submission not approved / U

Om åtgärderna du gjorde i `Re-submitted` bedöms att inte vara tillräckliga får du label `Submission not approved` och `U` i Canvas. Det kommer finnas kommentarer som förklara felet och läraren kommer stänga din PR.

I kmom10, vid större fel, ges inte komplettering. Då kan du få U direkt.

Din inlämning kommer inte rättas mer. Din PR stängs. Du behöver göra en ny PR till nästa rättningstillfälle. I [studieplanen](/#rekommenderad-studieplan) hittar du när nästa rättningstillfälle är.



## Vanliga fel

### Ett testfall gick fel

Om ett testfall går fel här, så saknar du de nyaste testfallen. Om du missat stegen i [Resultat och redovisning](/laromaterial/instruktion/resultat-och-redovisning/) så gör du det nu.

Så här ser det ut om du klickar under "Actions" i navbaren överst på GitHub:

import pr_fail1 from '@assets/pr/pr_fail1.png';

<Figure 
    src={pr_fail1}
    caption="Resultatet när du klickat på Actions."
/>

För att få mer information klickar du in på den action som gick rött, i det här fallet "Fix errors in testcase kmom01". Då ser det ut så här:

import pr_fail2 from '@assets/pr/pr_fail2.png';

<Figure 
    src={pr_fail2}
    caption="Resultatet av Action: Fix errors in testcase kmom01."
/>

För att få mer information om felet, klickar du på "call-on-tag / on-tag / run-kmoms (tests/kmom01/convert)" under **Annotations**. Då ser det ut så här:  

import pr_fail3 from '@assets/pr/pr_fail3.png';

<Figure 
    src={pr_fail3}
    caption="Resultatet av Action: Fix errors in testcase kmom01."
/>

Nu ser jag att ett testfall gått fel, som jag missat i testerna lokalt. Då gör jag enligt [instruktionerna under Fixa dina fel](#fixa-dina-fel).

### Gammal tag

Din senaste kod ska vara taggad med en ny tag, annars klagar valideringen. Se hur i [Resultat och redovisning](/laromaterial/instruktion/resultat-och-redovisning/#tagga-och-pusha-ditt-repo).
