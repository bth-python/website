## Teori

- Beskriv med egna ord vad en funktion (inom programmering) är.  
*En separat körbar del som löser en uppgift*  

- Vad finns det för förfelar med att använda funktioner.  
*Kan lösa problem som är användbara i flera sammanhang*  
*Får en bra läsbarhet*
*Får en bra struktur på programmen*

- Vad förutom ett namn behöver ingå i en funktion?  
*parameterlista och kropp*  

- Ge exempel på minst två funktioner som du/ni använt hittills och som du/ni inte definierat själv.  
*print(...)*  
*input(...)*
*len(...)*

- Vilken är regeln, dvs syntaxen, för hur en funktion måste skrivas i Python?  
*def namn(< parameterlista >):*  
*< en tab > sats*  
...  
*< en tab > sats*  

- Vad gäller för namngivning av funktioner i Python?  
*samma som för variabler, dvs liten bokstav först och _ mellan varje nytt ord*  

- Vad händer om man ger flera funktioner samma namn i Python?  
*Så snart en ny funktion med samma namn definieras "glöms" den tidigare, ex*  
```
def func(a):
    return a;

func(33)

def func():
    return "hello"


func()
func(44) <- här blir det fel eftersom func(a) har skrivits över med func()
```

- Vad menas med ett **funktionsanrop** och vad sker vid ett sådant?  
*körningen hoppar till den angivna funktionen, parametrarna tilldelas/initieras med argumenten (de resultat evalueringen av dessa ger), och satserna i kroppen exekveras, och eventuellt returneras ett värde. Därefter hoppas körningen till baka till anropet*  

- Förklara skillnaden på **parameter** och **argument**.  
*parameter är namnen på den variabler som skrivs i parameterlistan*   
*argument är de värden (via uttryck, variabler, ...) som anges vid anrop*  

- Vad menas med **default-argument** och hur skrivs sådana i Python? Ge exempel  
*När en parameter ges ett defaultvärde om det vid anropet inte får ett argument*  

- Om en funktion har de 3 parametrarna param1, param2 och param3 (i denna ordning)? Hur många olika möjligheter finns det för att ge defaultargument? Ge exempel på parameterlistor för varje möjlighet.  
*(param1 = 1, param2 = 2, param3 = 3)*  
*(param1, param2 = 2, param3 = 3)*  
*(param1, param2, param3 = 3)*  

- Vad är en **lokal variabel** i sammanhanget funktion?  
*en variabel som är deklarerad inom en funktion*  
*dess räckvidd är bara inom funktionen*

- Kan flera funktioner ha samma namn på sina lokala variabler?  
*ja, eftersom de är lokala inom funktionen och inte kända utanför*

- Kan en funktion anropa en annan funktion?  
*Ja*

### Hur kod funkar

1. Beskriv skillnaden på func01 och func02 nedan och visa hur respektive funktion anropas med målet att texten "I'm a function" ska skrivas ut.    
```python
def func01():
    print("I'm a function")
```
```python
def func02():
    return "I'm a function"
```
  
*func01() - gör en utskrift av strängen "I'm a function"*  
*func02() - returnerar strängen "I'm a function"*
*om func01() exekveras blir det en utskrift men för func02() behöves print(func01()) i så fall* 

2. Vad är det som är fel i func03() resp func04() nedan?  

```python
def func03():
    return "I'm a function"
    str = "Bye"
    print(str)
```  
*kod efter return, vilken inte exekveras eftersom return gör att körningen lämnar funktionen*  

```python
def func04(what):
print("I'm a function")
    print(what)
```  
*första print är inte indenterad/tabulerad*  

3. Utgå från kod-avsnittet  

```python
def func05(start, end = 10):
    total = 0
    for i in range(start, end):
        total += i
    return total

def func06():
    first = func05(10, 30)
    print(f"{first} and {func05(4)}")
    
```
Ange vad som är:
- anrop - *func05(10,30) och func05(4) samt range(start, end) och print(f"....")*
- lokal variabel - *total i func05 och first i func06, samt i inom for-loppen i func05*
- parameter - *start och end i func05*
- argument - *start och end i anropen av range(..) i func05, 10 och 30 i anropet av func05 inom func06, 4 i anropet av func05 inom func06 samt hela den formaterade strängen i anropet av print*
- defaultargument - *end = 10 i parameterlistan i func05*

### <Icon name="open-book" class="inline-icon" label="Interpret code" /> Tolka kod


5. Anta koda-avsnittet nedan  

```python
def func07(value):
    value += 4

nr = 10
func07(nr)
print(nr)
```
Vad blir det för utskrift?  
*10*

6. Anta koda-avsnittet nedan  

```python
def func08(phrase):
    phrase += " is fun!"
    return phrase

string = "Python"
print(func08(string))
print(string)
```
Vad blir det för utskrift?  
*Python is fun!*  
*Python*


7. Anta kod-avsnittet nedan

```python
def func09(param):
    result = "Number"
    if type(param) == str:
        result = "Text"
    elif type(param) == bool:
        result = "Boolean"
    return result

result = func09(44)
print(result)
result = func09("True")
print(result)
```
Vad blir det för utskrift?  
*Number*  
*Text*

## <Icon name="seti:python" class="inline-icon" label="Write code" /> Praktisk uppgift


8. Skriv funktioner enligt    

    - *average* som tar emot tre tal, beräknar och returnerar medelvärdet av dessa.  
    ```python
    def average(nr1, nr2, nr3):
        return (nr1 + nr2 + nr3)/3
    ```
    - *nr_of_100_bills* som tar emot ett heltal motsvarande en summa pengar och returnerar antalet 100-lappar denna kan växlas till. Ex-vis ger argumentet 765 returen 7  
    ```python
    def nr_of_100_bills(amount):
        return amount/100
    ```
    - *all_odd_numbers* som tar emot tal som representerar ett startvärde respektive ett slutvärde och som returnerar en sträng innehållande alla udda heltal inom mellan start- och slutvärdet. De udda talen ska vara separerade med komma. Exempelvis resulterar startvärdet 2 och slutvärdet 11 i att strängen "3, 5, 7, 9, 11" returneras.  
    ```python
    def all_odd_numbers(start, end):
    result = ""
    for nr in range(start, end+1):
        if nr % 2 != 0:
            result += str(nr) + ','
    return result
    ```
    - *is_leap_year* som tar emot ett årtal och returnerar sant eller falskt beroende på om årtalen motsvarar ett skottår eller ej. Ett skottår är årtal som är jämnt delbara med 400 eller jämnt delbart med 4 men inte med 100. Exempel på skottår är 1996, 2000, 2024. Exempel på år som inte är skottår är 1900 och 2023.  
     ```python
    def is_leap_year(year):
        return year % 400 == 0 or year %4 == 0 and year % 100 != 0
    ```


9. Gör en funktion namngiven *menu* som presenterar menyn  

    1. Beräkna medelvärdet av 3 heltal
    2. Beräkna antalet 100-lappar för en penga-summa
    3. Presentera alla udda heltal inom ett intervall
    4. Avgör om ett år är ett skottår eller ej
    0. Avsluta

    och låter användaren mata in sitt val, vilket returneras.

    ```python
    def menu():
        print("1: Beräkna medelvärdet av 3 heltal")
        print("2: Beräkna antalet 100-lappar för en penga-summa")
        print("3: Presentera alla udda heltal inom ett intervall")
        print("4: Avgör om ett år är ett skottår eller ej")
        print("0. Avsluta")
        return int(input("Ditt val" ))

    ```

10. Använd alla funktioner från uppgift 8. och 9. och gör ett program som presenterar menyn (från uppgift 9) och låter användaren mata in sitt val vilket hanteras av funktioner från uppgift 8. Användaren ska kunna göra nya val tills dess att hen väljer menyval 5. 

    ```python
    user_choice = menu()
    while user_choice != 0:
        if user_choice == 1:
         print("Input 3 integers")
          nr1 = int(input())
          nr2 = int(input())
          nr3 = int(input())
          print(f"The average of {nr1}, {nr2} and {nr3} is {round(average(nr1, nr2, nr3), 2)}")
        if ...
        ...
        user_choice = menu()        
    
    ```

